void GMOD::setCameraProps(const int& camId, 
                          const int& camResX, 
                          const int& camResY, 
                          const int& camFps) {
  this->camId = camId;
  this->camResX = camResX;
  this->camResY = camResY;
  this->camFps = camFps;
}